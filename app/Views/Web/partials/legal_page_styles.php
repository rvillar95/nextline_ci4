<style>
.legal-hero-ns {
    background: linear-gradient(135deg, var(--brand-green-primary) 0%, var(--brand-green-dark) 100%);
    padding: 100px 0 60px;
    margin-top: 0;
}
.legal-body-ns {
    padding: 3rem 0 4rem;
    background: var(--bg-light);
}
.legal-card-ns {
    background: var(--bg-card);
    padding: 2.5rem;
    border-radius: 20px;
    border: 1px solid var(--border-light);
    box-shadow: 0 8px 32px rgba(15, 23, 42, 0.06);
}
.legal-card-ns h2 {
    color: var(--text-main);
    font-size: 1.35rem;
    font-weight: 800;
    margin-top: 2rem;
    margin-bottom: 1rem;
}
.legal-card-ns h2:first-of-type { margin-top: 0; }
.legal-card-ns p, .legal-card-ns li {
    color: var(--text-muted);
    line-height: 1.75;
}
.legal-card-ns ul { margin-bottom: 1.25rem; padding-left: 1.25rem; }
.legal-contact-box {
    background: var(--surface-green-soft);
    border-left: 4px solid var(--brand-green-primary);
    padding: 1.25rem 1.5rem;
    border-radius: 12px;
    margin-top: 1rem;
}
@media (max-width: 768px) {
    .legal-card-ns { padding: 1.5rem 1.25rem; }
}
</style>
