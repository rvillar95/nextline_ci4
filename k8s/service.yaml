apiVersion: v1
kind: Service
metadata:
  name: nutrinext-api
  labels:
    app: nutrinext-api
spec:
  type: LoadBalancer
  selector:
    app: nutrinext-api
  ports:
    - port: 80
      targetPort: 80

